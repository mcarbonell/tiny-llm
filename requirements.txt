# =====================================================
# TinyThinker — Dependencias del proyecto
# Pinned: 2026-04-06  (pip freeze)
# =====================================================

# Core ML
torch==2.10.0
numpy==2.4.2

# Tokenización y datasets
tokenizers==0.22.2
datasets==4.8.4
transformers==5.5.0

# Utilidades generales
tqdm==4.67.3
python-dotenv==1.2.1
PyYAML==6.0.3

# Generación de datos sintéticos (opcional)
openai==2.16.0

# Herramienta de búsqueda (tool-calling)
duckduckgo-search>=6.3.0
